#set ($services = $class.allServices)

#foreach ($service in $services)
#set ($serviceType = "Session")
#set ($serviceName = $service.name)
#set ($packageName = $service.packageName)
#set ($ejbRefName = "ejb/${packageName}/${serviceName}")

  <ejb-ref>
    <ejb-ref-name>${ejbRefName}/Remote</ejb-ref-name>
    <ejb-ref-type>$serviceType<ejb-ref-type>
    <home>${packageName}.${serviceName}Home</home>
    <remote>${packageName}.${serviceName}</remote>
  <ejb-ref>
#end