#set ($ejbRefs = $class.allEjbRefs)

#foreach ($ejbRef in $ejbRefs)
#set ($serviceName = $ejbRef.name)
#set ($packageName = $ejbRef.packageName)
#set ($ejbRefName = "ejb/${packageName}/${serviceName}")
#set ($ejbRefType = "Session")
  <ejb-ref>
    <ejb-ref-name>${ejbRefName}/Remote</ejb-ref-name>
    <ejb-ref-type>$ejbRefType<ejb-ref-type>
    <home>${packageName}.${serviceName}Home</local-home>
    <remote>${packageName}.${serviceName}</local>
  <ejb-ref>
#end