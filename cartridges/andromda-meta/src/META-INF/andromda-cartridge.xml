<cartridge name="meta">

    <!-- define the template objects that are made availble to the template -->
    <templateObject name="str" className="org.andromda.core.common.StringUtilsHelper"/>

    <!-- the template definitions -->
    <template
        sheet="templates/MetafacadeDecorator.vsl"
        outputPattern="{0}/{1}Decorator.java"
        outlet="facades"
        overWrite="true">
        <stereotype name="Decorator" />
        <stereotype name="metafacade" />
    </template>

    <template
        sheet="templates/MetafacadeDecoratorImpl.vsl"
        outputPattern="{0}/{1}DecoratorImpl.java"
        outlet="facade-impls"
        overWrite="false">
        <stereotype name="Decorator" />
        <stereotype name="metafacade" />
    </template>

    <template
        sheet="templates/MetafacadeInterface.vsl"
        outputPattern="$myOutputFile"
        outlet="facade-interfaces"
        overWrite="true">
        <stereotype name="Decorator" />
        <stereotype name="metafacade" />
    </template>

</cartridge>
